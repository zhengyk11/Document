# 朴素贝叶斯实验报告

2011011384 郑玉昆 计34

## 1. 算法实现

### 1.1 变量和方法

#### 1.1.1 变量

代码中使用了大量的hashmap用来存储每个属性分量与类别属性的关系对。

```java
    //>50K
    Map<Integer, Integer> Age_r = new HashMap<>();
    Map<String, Integer> WorkClass_r = new HashMap<>();
    Map<Integer, Integer> Fnlwgt_r = new HashMap<>();
    Map<String, Integer> Education_r = new HashMap<>();
    Map<Integer, Integer> EducationNum_r = new HashMap<>();
    Map<String, Integer> MaritalStatus_r = new HashMap<>();
    Map<String, Integer> Occupation_r = new HashMap<>();
    Map<String, Integer> Relationship_r = new HashMap<>();
    Map<String, Integer> Race_r = new HashMap<>();
    Map<String, Integer> Sex_r = new HashMap<>();
    Map<Integer, Integer> CapitalGain_r = new HashMap<>();
    Map<Integer, Integer> CapitalLoss_r = new HashMap<>();
    Map<Integer, Integer> HoursPerWeek_r = new HashMap<>();
    Map<String, Integer> NativeCountry_r = new HashMap<>();

    //<=50K
    Map<Integer, Integer> Age_p = new HashMap<>();
    Map<String, Integer> WorkClass_p = new HashMap<>();
    Map<Integer, Integer> Fnlwgt_p = new HashMap<>();
    Map<String, Integer> Education_p = new HashMap<>();
    Map<Integer, Integer> EducationNum_p = new HashMap<>();
    Map<String, Integer> MaritalStatus_p = new HashMap<>();
    Map<String, Integer> Occupation_p = new HashMap<>();
    Map<String, Integer> Relationship_p = new HashMap<>();
    Map<String, Integer> Race_p = new HashMap<>();
    Map<String, Integer> Sex_p = new HashMap<>();
    Map<Integer, Integer> CapitalGain_p = new HashMap<>();
    Map<Integer, Integer> CapitalLoss_p = new HashMap<>();
    Map<Integer, Integer> HoursPerWeek_p = new HashMap<>();
    Map<String, Integer> NativeCountry_p = new HashMap<>();
```

### 1.2 细节设计

## 2. 性能评估

## 3. 结果分析
